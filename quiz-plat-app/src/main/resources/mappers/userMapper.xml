<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.quiz.mapper.userMapper">

 	<select id="insertUser" parameterType="userDto">
		INSERT INTO USER(USER_ID, NICKNAME, PWD, REG_DIV_CD, REGPE_ID, REG_DTS, MOD_DTS) VALUES(#{user_id}, #{nickname}, #{pwd}, #{reg_div_cd}, #{regpe_id}, now(), now())
    </select>
    
    <select id="getNickname" resultType="String">
    	SELECT  NICKNAME
		FROM    NICKNAME
		WHERE   1 = 1
		AND     USE_YN = 'N'
		ORDER BY RAND() LIMIT 1
    </select>    
    
    <select id="chekUserId" parameterType="String" resultType="int">
    	SELECT COUNT(USER_ID)
		FROM   USER
		WHERE  1 = 1
		AND    USER_ID = #{user_id}
    </select>
    
    <select id="updateNickname" parameterType="String">
    	UPDATE NICKNAME
    	SET    USE_YN = 'Y'
    	WHERE  1 = 1
    	AND    NICKNAME = #{nickname}
    </select>
    
    <select id="chekOurUser" parameterType="userDto" resultType="int">
    	SELECT COUNT(USER_ID)
		FROM   USER
		WHERE  1 = 1
		AND    USER_ID = #{user_id}
		AND    PWD     = #{pwd}
    </select>
    
</mapper>